import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
    container: {
        height: 10,
        borderRadius: 5,
        backgroundColor: '#f0f0f0',
        overflow: 'hidden',
    },
    progressBar: {
        height: '100%',
        borderRadius: 5,
    },
})
