import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
    container: {
        position: 'absolute',
        bottom: 50,
        left: 0,
        right: 0,
        padding: 10,
        borderRadius: 5,
        alignItems: 'center',
    },
    message: {
        fontSize: 16,
    },
})
